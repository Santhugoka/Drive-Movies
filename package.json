{
  "name": "new-folder-(3)",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "if not exist www mkdir www && copy index.html www\\index.html && copy style.css www\\style.css && copy script.js www\\script.js",
    "sync": "npm run build && npx cap copy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.3.3",
    "@capacitor/cli": "^8.3.3",
    "@capacitor/core": "^8.3.3"
  }
}
